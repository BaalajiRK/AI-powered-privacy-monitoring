package.name = guarddogapp
package.domain = org.security

# Permissions your guard dog needs from Android
android.permissions = INTERNET, ACCESS_NETWORK_STATE, ACCESS_FINE_LOCATION, RECORD_AUDIO, FOREGROUND_SERVICE

# Python packages needed
requirements = python3, kivy, pyjnius, cryptography, requests, websockets
